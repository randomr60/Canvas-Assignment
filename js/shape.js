"use strict";

class Shape {
    
    constructor(x_pos, y_pos, length, width) {
        this.currentX = x_pos;
        this.currentY = y_pos;
        this.length = length;
        this.width = width;
    }
    

    
    
}